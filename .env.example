# GitHub personal access token (required)
# Needs read:user and public_repo scopes only
GITHUB_TOKEN=ghp_your_token_here

# Optional: identify your instance to GitHub's API
GITHUB_USER_AGENT=ContribLens/1.0

# Optional: port to listen on (default: 3000)
PORT=3000
